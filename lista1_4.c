//exercicio que não precisa programar será resolvido só com comentários

/*Exercício 4. Dado o trecho abaixo, determine o valor impresso por cada printf, sem executar o código. Em seguida, compile e execute o programa para conferir suas respostas.

int v[5] = {2, 4, 6, 8, 10};
int *p = v;

printf("%d\n", *p); = 2, ele pega a primeira posição do vetor, que é 2

printf("%d\n", *(p + 2)); = 6, ele pega a posição 2 do vetor, que é 6

printf("%d\n", p[3]); = 8, ele pega a posição 3 do vetor, que é 8

printf("%d\n", *p + 1); = 3, ele pega a primeira posição do vetor, que é 2, e soma 1

printf("%d\n", *(v + 4)); = 10, ele pega a posição 4 do vetor, que é 10
*/